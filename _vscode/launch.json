{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "lldb", // "cppdbg",
      "request": "launch",
      "name": "Debug",
      "program": "${fileDirname}/${fileBasenameNoExtension}",
      "args": [],
      "cwd": "${workspaceFolder}",
      // "MIMode": "gdb",
      // "miDebuggerPath": "/usr/bin/gdb",
      "preLaunchTask": "make",
    }
  ]
}
